@model IEnumerable<RoSAT.Models.Event>

@if (Model.Count() > 0)
{
    <div class="container tableIt wrapper">
        <table align='center' id="data_table">
            <tr>
                <th>
                    Name
                </th>
                <th>
                    Position
                </th>
                <th>
                    Certificate
                </th>
                <th>
                    Level
                </th>
                <th>
                    Type
                </th>
                <th></th>
            </tr>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Position)
                    </td>
                    <td>
                        @if (item.CertificatePhoto != null)
                        {
                            using (Html.BeginForm("ViewCertificate", "Events", FormMethod.Post, new { target = "_blank" }))
                            {
                                @Html.Hidden("id", item.Id)
                                <input type="submit" class="btn-link" value="View Certificate" />
                            }
                        }
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.EventLevel.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.EventType.Name)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "EditEvent", new { id = item.Id }) |
                        @Html.ActionLink("Delete", "DeleteEvent", new { id = item.Id })
                    </td>
                </tr>
            }

        </table>
    </div>

}